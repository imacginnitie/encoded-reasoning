
matrix:
  models:
    - provider: "anthropic"
      name: "claude-opus-4-5"
  ciphers:
    - type: "identity"
      params: {}
    - type: "emojispeak"
      params: {}
    - type: "direct"
      params: {}
    - type: "filler"
      params: {}
      filler:
        type: "counting"
        count: 300

prompt:
  num_examples: 4  # k: number of examples in few-shot prompt

dataset:
  name: "aime2025"  # Dataset name: "math500" or "aime2025"
  num_test_examples: 30  # n: number of examples to test on

experiment:
  output_dir: "results"  # base directory for results (will create subdirs for each model/cipher combination)

