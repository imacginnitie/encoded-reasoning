# Example: Direct answering mode - no chain-of-thought, only boxed answer
model:
  provider: "anthropic"
  name: "claude-3-5-sonnet-20241022"

cipher:
  type: "direct"  # Direct answering mode - no CoT, only final answer
  params: {}

prompt:
  num_examples: 5

dataset:
  name: "math500"
  num_test_examples: 100
  train_split: 0.7
  val_split: 0.15
  test_split: 0.15

experiment:
  output_dir: "results"

